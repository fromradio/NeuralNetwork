# coding utf-8

import numpy as np


# generate a set of data that can be separated by a hyper plane
def data_generate(m,n):
	# generate a plane
	hyper_plane = np.random.rand(m)
	for i in range(n):
		# 
		pass 

if __name__ == '__main__':
	data_generate(100)